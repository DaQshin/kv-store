name: Protocol Parse Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run protocol tests
        run: ./tests/protocol/e2e_test
