name: Protocol Parse Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compile test
        run: g++ ./tests/protocol/parse.cpp -o parse_test

      - name: Run test
        run: ./parse_test
